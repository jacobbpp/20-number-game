// Single Cloudflare Worker backing both the community "usual spot" stats
// and the leaderboard — same D1 database, split by table.
export const API_BASE = 'https://order20-community-stats.tb-dev.workers.dev'
